# king· — agency website

Static HTML/React site for King Media.

## Dev server

```
node serve.mjs
```

Serves the project at `http://localhost:3000`. Routes: `/` → index.html, `/privacy` → privacy.html, `/terms` → terms.html.
